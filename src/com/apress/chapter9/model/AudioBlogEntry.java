package com.apress.chapter9.model;

/**
 * The AudioBlogEntry extends MediaBlogEntry and represents an entry that
 * has an audio component in addition to a text component
 */
public class AudioBlogEntry extends MediaBlogEntry {
  
  public AudioBlogEntry(User user) {
    super(user);
  }  
}
