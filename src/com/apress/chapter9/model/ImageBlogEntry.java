package com.apress.chapter9.model;

/**
 * The ImageBlogEntry extends MediaBlogEntry and represents an entry that
 * has an image component in addition to a text component
 */
public class ImageBlogEntry extends MediaBlogEntry {
  
  public ImageBlogEntry(User user) {
    super(user);
  }  
}

